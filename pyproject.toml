[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "advent-of-code"
version = "0.0.1"
dependencies = [
    "pytest",
    "regex",
]

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]